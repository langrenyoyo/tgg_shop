# 站点端闭环测试报告

测试日期：2026-09-22  
测试范围：站点工作人员登录、收货、异常拦截、用户提货核验，以及用户端/后台订单状态同步。

## 结论

P0 的“登录 → 本站收货 → 待提货 → 取货码核验 → 已提货/已完成”服务端闭环通过。订单完成收货后，共享订单记录会带有 `stationStatus=ready`；核验成功后同步为 `stationStatus=picked_up`、`fulfillmentStatus=picked_up`、`status=completed`。

## 验证结果

| 场景 | 结果 |
| --- | --- |
| 站点账号登录并建立独立会话 | 通过 |
| 只访问授权站点订单 | 通过 |
| 错站收货 | 拒绝 |
| 未支付/非自提/退款中/关闭订单收货或提货 | 拒绝（履约规则及站点规则测试通过） |
| 实收商品数量不一致 | 拒绝 |
| 收货后换新幂等键再次收货 | 拒绝，不覆盖原储位和状态 |
| 同一幂等键重试 | 返回第一次结果 |
| 未收货直接提货 | 拒绝 |
| 错误取货码 | 拒绝 |
| 正确取货码提货 | 订单完成并写入站点日志 |
| 用户订单、后台订单读取站点状态 | 通过 |
| SQLite smoke 与重启数据链路 | 通过 |

## 自动化证据

- 站点、履约、认证相关单元测试：13 项通过。
- 浏览器真实点击闭环：商城用户下单 → 站点收货 → 用户/后台显示已到站 → 错码重试 → 正确核验 → 用户/后台显示已完成，全部通过。
- `node test/smoke.test.js sqlite`：通过。
- OpenAPI 检查、数据库迁移检查、站点前端和后端 JavaScript 语法检查：通过。
- 独立内存服务 API 实测：错站、错数量、重复收货、正确提货及用户/后台同步均符合预期。

## 本次修复

- 收货和提货将站点履约状态同步到共享订单记录。
- 收货校验作业站点、实收明细和已收货状态。
- 幂等键限定操作人、订单和操作类型，冲突返回 409。
- 日志使用字符串结果并单独保存响应，避免日志页面显示 `[object Object]`。
- 前端提交期间禁用重复提交；退出登录调用服务端注销并清理页面状态；只有 401 才清理会话。

## 尚未达到的范围

站点网页已接入摄像头扫码入口，支持二维码及常见条码格式；浏览器不支持 `BarcodeDetector`、未授权摄像头或非 HTTPS 环境时会自动保留手动输入。真实设备兼容性仍需在目标手机/扫码枪上做现场验收。批量收货、图片异常凭证、逾期通知和多站点切换属于后续 P1/P2 范围。
