# TGG Shop 网页端测试流程文档

> 版本：v1.0  
> 适用范围：用户网页端 `dev/frontend/user/`  及其后端接口  
> 测试目标：确认桌面/移动浏览器中的商品、会员、订单、积分和首页运营内容闭环。

## 1. 测试准备

### 1.1 启动和地址

```powershell
cd dev/backend
npm run dev
```

- 用户网页：`http://127.0.0.1:5177/user?skipSplash=1`
- 健康检查：`http://127.0.0.1:5177/api/health`
- 推荐浏览器：最新版 Chrome/Edge；同时使用移动设备模拟器检查窄屏布局。
- 每次切换测试账号，清理对应页面的 localStorage 或使用无痕窗口，避免旧购物车和 token 干扰。

### 1.2 测试账号

| 账号 | 密码 | 验证内容 |
| --- | --- | --- |
| `u_1001` | `123456` | 会员现金购买、现金补差 |
| `u_1002` | `123456` | 普通用户纯积分兑换、会员拦截 |

## 2. 冒烟流程

1. 打开用户网页，确认首页加载完成，顶部导航、Banner、推荐商品和纯积分入口可见。
2. 点击分类，确认商品列表、分类筛选和现金/纯积分模式按钮可用。
3. 打开商品详情，确认商品主图、规格、介绍、库存和价格显示正确。
4. 加入购物车并进入结算，确认订单确认页可加载。
5. 使用 `u_1002` 完成纯积分自提订单，刷新页面后订单仍存在。
6. 使用 `u_1001` 完成现金商品购买，确认支付结果、订单列表和库存变化。

## 3. 功能测试流程

### 3.1 首页运营内容

1. 检查位置、自提点、配送承诺、服务标签和推荐商品。
2. 检查首页 Banner 标题、副标题、图片和跳转商品是否来自后台最新配置。
3. 下架 Banner 关联商品后刷新网页，确认 Banner 不再提供购买入口。
4. 检查活动入口可跳转到会员、积分兑换、签到、任务和邀请页面。
5. 修改后台首页配置后，用户网页重新进入或刷新应显示新内容。

### 3.2 商品浏览和筛选

1. 分类页选择“全部”、自定义分类、现金和纯积分模式，确认结果正确。
2. 草稿、已下架商品不得出现在公开商品列表、首页推荐和公开详情页。
3. 商品详情显示单位、介绍、会员购买规则和纯积分规则。
4. 商品名、标签、分类含特殊字符时，确认页面只显示文本，不执行 HTML/脚本。
5. 库存为 0 时，购买按钮禁用或提示售罄；刷新后状态保持一致。

### 3.3 购物车和结算

1. 添加、删除、增减商品数量，检查小计、总计和浮动购物车数量。
2. 普通用户现金商品点击购买，确认出现会员开通提示并阻止现金下单。
3. 普通用户纯积分商品下单，确认没有现金补差选项。
4. 会员现金购买检查现金支付；会员积分不足时检查现金补差入口。
5. 自提结算检查自提点和取货码；配送结算检查地址、配送费和时段。
6. 重复点击提交、刷新提交结果页，确认订单只创建一次。

### 3.4 订单、支付和售后

1. 订单列表检查状态筛选、订单详情、支付记录和履约信息。
2. 检查待支付订单的支付、取消和超时处理。
3. 检查自提订单核销码展示，配送订单检查收货流程。
4. 在可退款状态提交退款，确认现金和积分金额分开显示。
5. 重复进入退款页面或重复提交，确认不会重复退款。
6. 订单完成/取消/退款后，商品库存、用户积分和支付记录与后台一致。

### 3.5 积分和会员

1. 查看积分余额、积分流水和排行榜，确认余额与流水末笔一致。
2. 签到、任务、邀请产生的积分应在刷新后保留，并能追溯来源。
3. 会员开通、续费后检查会员有效期、会员标识和现金购买权限。
4. 普通用户积分不足纯积分兑换时，只提示积分不足，不引导现金支付。

## 4. 浏览器和异常测试

- Chrome/Edge 桌面宽度 1440、1024、760、375 像素检查无横向滚动和按钮遮挡。
- 断网、接口 4xx/5xx、慢网环境下检查 toast/错误信息和重试能力。
- 清空 localStorage、token 过期、多个标签页同时修改购物车，确认会话和数据处理合理。
- 检查浏览器控制台无新增 `exception`、`console.error` 或资源 404。
- 检查图片上传后的 Banner 和商品主图在刷新、重新登录后仍可访问。

## 5. 验收标准

- 用户网页可完成浏览 → 加购 → 结算 → 下单 → 支付/积分扣减 → 订单查询。
- 首页后台配置、商品上下架、图片和库存变化能同步到公开网页。
- 普通用户、会员、纯积分商品的权限和支付规则正确。
- 下架商品停止新交易，已有订单和历史记录保留。
- 桌面和移动布局无阻塞问题，关键页面无未处理运行时错误。

## 6. 测试记录

| 日期 | 浏览器/尺寸 | 账号 | 用例范围 | 通过数/失败数 | 缺陷编号 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |
