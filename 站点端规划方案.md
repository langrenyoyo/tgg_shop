# TGG Shop 站点端规划方案

> 版本：V1.0  
> 日期：2026-09-22  
> 目标：在现有小程序、用户网页和运营后台基础上，规划一个可用于 PC、平板和手机浏览器的用户站点。

## 1. 项目现状分析

当前项目已经具备三类基础能力：

1. **用户网页端**：位于 `dev/frontend/user/`，已经覆盖首页、商品、购物车、结算、订单、会员、积分、签到、任务、邀请、地址、支付记录、排行榜、提现和客服等功能。当前页面使用 390px 移动端外壳，更接近移动 H5 演示，桌面端空间利用率、导航层级和搜索体验不足。
2. **微信小程序端**：位于 `wechat-miniprogram/`，页面和用户网页共用后端业务接口，已补齐首页运营、商品规则、地址管理和用户服务入口。
3. **后台运营端**：位于 `dev/frontend/admin/`，用于仪表盘、商品、订单、配送、首页运营、会员、财务、权限和异常补偿。站点端只消费用户接口，不应直接复用后台接口或绕过权限。

后端已经提供统一的用户 API，包括 `/api/home`、`/api/products`、`/api/orders`、`/api/payments`、`/api/addresses`、`/api/points-ledger`、`/api/ranking`、`/api/withdrawals`、`/api/tickets`、会员、签到、任务和邀请接口。站点端的第一阶段应优先复用这些接口，避免为三端重新实现订单、支付和积分规则。

## 2. 站点端定位

站点端定位为 **TGG Shop 用户商城站**，服务对象是普通用户和会员用户，主要承担：

- PC 浏览器购物和运营活动承接；
- 搜索、分类、商品详情和订单管理；
- 会员、纯积分兑换、签到、任务和邀请等增长功能；
- 统一的账户、地址、支付、售后和客服入口；
- 通过链接承接小程序二维码、活动投放和后台配置的 Banner。

站点端不包含后台商品维护、权限配置、财务审批、订单人工发货和配送调度，这些继续由 `/admin` 运营端负责。代理核销也保持独立的运营流程，不在用户站点内开放。

## 3. 产品结构

### 3.1 顶部导航

桌面端使用固定顶部导航，包含：

- TGG Shop 品牌与首页入口；
- 商品分类；
- 纯积分兑换；
- 任务赚积分；
- 会员中心；
- 自提点和配送说明；
- 搜索框；
- 消息、购物车和个人中心。

移动浏览器使用折叠菜单，保留首页、分类、购物车、我的四个主要入口，并在首页突出签到、任务和纯积分兑换。

### 3.2 页面清单

| 优先级 | 页面 | 主要内容 | 主要接口 |
| --- | --- | --- | --- |
| P0 | 首页 `/site/` | 定位/自提点、配送承诺、Banner、活动入口、分类、推荐商品 | `/api/home`、`/api/config`、`/api/products` |
| P0 | 商品列表 `/site/products` | 分类、关键字、价格/积分模式、库存状态、分页 | `/api/products`、`/api/points-exchange` |
| P0 | 商品详情 `/site/products/:id` | 图片、规格、产地、库存、会员现金价、积分价、购买规则 | `/api/products/:id`、`/api/me` |
| P0 | 购物车 `/site/cart` | 数量、失效商品、金额/积分汇总、去结算 | 本地购物车、`/api/products` |
| P0 | 结算 `/site/checkout` | 纯积分/现金模式、地址、自提点、配送方式、幂等提交 | `/api/me`、`/api/config`、`/api/addresses`、`/api/orders` |
| P0 | 登录 `/site/login` | 用户登录、退出、刷新会话 | `/api/auth/login`、`/api/auth/refresh`、`/api/auth/logout` |
| P0 | 订单 `/site/orders` | 全部、待支付、履约中、已完成、售后 | `/api/orders`、支付/退款接口 |
| P0 | 订单详情 `/site/orders/:id` | 状态机、支付、取货码、配送、收货、退款 | `/api/orders/:id/*`、`/api/payments` |
| P0 | 个人中心 `/site/account` | 会员状态、积分、订单、地址、支付记录、客服 | `/api/me`、账户接口 |
| P1 | 会员 `/site/membership` | 月会员权益、开通/续费、支付恢复 | `/api/member/*`、`/api/payments` |
| P1 | 积分中心 `/site/points` | 积分流水、排行榜、提现 | `/api/points-ledger`、`/api/ranking`、`/api/withdrawals` |
| P1 | 任务与签到 `/site/earn` | 任务详情、提交记录、签到广告流程、抽奖 | `/api/tasks`、`/api/submissions`、`/api/signin/*` |
| P1 | 邀请 `/site/invite` | 邀请码、好友、贡献积分、分享链接 | `/api/invite/*` |
| P1 | 地址 `/site/addresses` | 新增、编辑、默认地址、删除 | `/api/addresses` |
| P1 | 服务中心 `/site/service` | 客服、反馈、商务合作、招聘、自提点 | `/api/tickets`、`/api/pickup-sites` |
| P2 | 搜索结果 `/site/search` | 联想、热门搜索、无结果推荐、搜索历史 | 首期复用 `/api/products`，后续增加搜索 API |
| P2 | 促销专题 `/site/campaign/:id` | 新人礼包、满减、限时活动、会员专享 | 后台活动 API（规划新增） |

## 4. 核心业务规则

站点端只负责展示和交互，最终规则由后端校验。所有端必须保持以下一致性：

- 普通用户不能现金购买，也不能使用现金补足积分差额；
- 纯积分商品不要求会员，不显示现金补差入口，积分不足时只能提示积分不足；
- 会员可以现金购买，也可以在积分不足时使用现金补差；
- 下架商品不能新建购买订单，已有订单继续履约；
- 现金订单、支付回调、退款、提现、积分扣减和库存锁定均使用幂等键；
- 订单状态按待支付、已支付/已扣积分、待履约、配送/待自提、已完成、退款中、已退款、已取消和异常补偿展示；
- 自提订单展示取货码，配送订单展示地址、配送状态和自建配送团队的履约信息；
- 退款需要区分现金和积分原路退回，用户只能提交申请，审批由后台财务完成；
- 敏感操作失败、网络中断或状态未知时，页面提供查询和重试，不重复创建订单或重复扣款。

## 5. 技术方案

### 5.1 目录和访问路径

建议新增独立目录：

```text
dev/frontend/site/
  index.html
  styles.css
  js/
    app.js
    api.js
    state.js
    router.js
    render.js
    components/
```

访问路径为 `http://localhost:5177/site`。后端静态路由新增 `/site` 映射，API 继续使用 `/api/*`。现有 `/user` 保留，作为移动端兼容页面和回归基准，不在第一阶段强行替换。

### 5.2 前端实现原则

- 第一阶段继续使用项目现有的原生 ES Module 和 CSS，不引入新的大型框架，减少构建和部署成本；
- 将现有用户网页的 API 封装、业务校验和通用渲染逻辑抽成共享模块，站点端只替换布局和路由；
- 桌面端使用响应式三栏/双栏布局，移动端在 768px 以下切换为单栏；
- 所有页面支持直接刷新和深链接，路由参数不得依赖页面前置状态；
- 图片优先使用后台上传的资源 URL，失败时显示可识别的占位和重试入口；
- 订单提交按钮在请求期间锁定，页面恢复时先查询未完成支付和订单状态；
- 页面展示文案以后台配置为准，禁止在前端硬编码会员价格、积分奖励和配送承诺。

### 5.3 需要补充的后端能力

P0 可以复用现有接口。为改善桌面站搜索和商品体验，后续增加：

```text
GET /api/categories
GET /api/search/suggest?keyword=
GET /api/search/products?keyword=&category=&sort=&page=&count=
GET /api/campaigns/:id
```

商品模型建议逐步补齐 `spec`、`unit`、`origin`、`shelfLife`、`storageType`、`monthlySales`、`limitPerUser`、`lowStockThreshold` 和 `serviceTags`。新增字段必须兼容旧商品，不能影响现有下单校验。

## 6. 视觉和交互规划

- 主色沿用现有 TGG 绿色体系，桌面背景使用浅灰，内容区域使用白色卡片；
- 首页首屏明确回答“卖什么、多久送到、如何购买、会员和积分有什么区别”；
- 商品卡同时显示商品图、名称、规格、库存标签、会员现金价和积分价；
- 普通用户看到现金商品时直接显示“开通月会员后购买”，纯积分商品显示“立即兑换”；
- 购物车顶部显示当前账户可用的支付模式，现金商品和纯积分商品混合时给出拆单提示；
- 结算页固定显示费用、积分、配送方式和提交结果，错误信息靠近对应字段；
- 订单详情使用时间线展示履约状态，支付、取消、收货、退款按钮按状态动态开放；
- 全站支持键盘导航、焦点态、表单错误提示、空状态、加载态和网络重试；
- 不把后台实现细节暴露给用户，用户只看到与当前决策相关的规则和结果。

## 7. 分阶段开发计划

### 阶段一：站点骨架和 P0 购物闭环（约 5 个工作日）

1. 新增 `/site` 静态入口、响应式布局、顶部导航、登录态和路由；
2. 完成首页、商品列表、商品详情、购物车和结算；
3. 接入现有商品、首页、地址、订单和支付接口；
4. 覆盖会员/普通用户、纯积分和现金订单三种关键场景；
5. 加入桌面端和移动浏览器的基础适配。

### 阶段二：账户和增长能力（约 4 个工作日）

1. 完成订单列表/详情、支付记录、退款申请和售后状态；
2. 完成会员、积分流水、排行榜、提现；
3. 完成任务、签到、邀请和提交记录；
4. 完成地址、客服、反馈、自提点等服务页面。

### 阶段三：运营增强（约 5 个工作日）

1. 增加分类、搜索联想、分页和无结果推荐；
2. 增加促销专题、新人礼包、满减和配送费规则；
3. 增加商品规格、低库存提示、销量和服务标签；
4. 增加埋点、转化漏斗、错误监控和站点性能优化。

## 8. 测试和验收标准

### 功能验收

- 未登录用户可以浏览首页、分类、商品和公开自提点；
- 登录后可以完成地址管理、购物车、纯积分订单和会员现金订单；
- 普通用户现金购买被拦截，会员开通后可以继续完成现金支付；
- 纯积分商品不出现现金补差入口，积分不足时不产生订单；
- 重复点击提交、刷新支付页、网络恢复后不会重复扣款或重复建单；
- 自提和配送订单分别展示正确的履约状态；
- 退款、提现和客服工单提交后能看到处理状态；
- 小程序、站点端和后台对同一商品、订单、库存、会员和积分数据保持一致。

### 技术验收

- 现有 `npm run test:unit`、小程序页面测试和后端 smoke 测试通过；
- 新增站点路由、API 失败重试、登录刷新和深链接测试；
- Chrome/Edge 桌面宽度 1440px、1024px、768px 和手机宽度 390px 无横向溢出；
- 无障碍检查覆盖表单标签、按钮名称、键盘焦点和颜色对比度；
- 站点端不能访问 `/api/admin/*`，后台权限变更不影响用户端接口；
- 生产环境启用 HTTPS、真实支付和真实回调前，必须单独进行联调验收。

## 9. 风险和决策

1. **现有用户网页逻辑与布局耦合较深**：先抽取 API 和业务规则，再改造视觉层，避免直接复制出两套订单逻辑。
2. **搜索和促销接口暂不完整**：P0 使用商品列表接口完成可用版本，搜索索引和活动中心放在 P2，避免阻塞商城闭环。
3. **桌面站与小程序能力不同**：站点支持链接分享、键盘操作和更长页面；支付、任务广告和微信能力必须保留降级提示，不能假设浏览器具备小程序 SDK。
4. **站点端不是后台替代品**：所有商品上架、首页图片、权限、退款审批和库存调整仍由运营后台完成，站点只读取发布后的结果。

## 10. 推荐首个可交付版本

建议先交付“站点端 V1 商城闭环”：首页、分类、商品详情、购物车、结算、登录、订单、账户和地址，覆盖纯积分兑换与会员现金购买两条主流程，部署在 `/site`，与现有 `/user` 并行运行。确认接口和业务验收稳定后，再迁移积分增长、客服和促销专题，避免一次性重写现有用户网页。
