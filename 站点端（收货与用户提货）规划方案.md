# TGG Shop 站点端（收货与用户提货）规划方案

> 版本：V1.0  
> 日期：2026-09-22  
> 定位：供自提点工作人员使用的履约端，负责货物到站、站内保管和用户提货核验。

## 1. 范围定义

站点端不是用户商城，也不是完整运营后台。它只服务于已配置的自提点和站点工作人员，核心闭环是：

```text
待到站订单 → 站点收货 → 站内待提 → 用户核验提货 → 订单完成
```

用户商城、小程序和站点端共用订单、商品、库存、支付和取货码数据；商品上架、价格修改、财务审批、权限管理和配送调度仍由运营后台负责。

## 2. 使用角色

| 角色 | 权限范围 |
| --- | --- |
| 站点管理员 | 查看本站订单、收货、异常登记、提货核验、查看本站操作日志 |
| 站点工作人员 | 执行收货和用户提货核验，不能修改价格、库存金额、退款和用户积分 |
| 运营管理员 | 配置自提点、分配站点人员、处理跨站异常和订单状态 |

站点人员必须绑定一个或多个自提点。所有查询和写操作按 `siteId` 隔离，禁止通过修改订单号访问其他站点数据。

## 3. 主要功能

### 3.1 站点工作台

- 今日待收货、运输中、已到站待提货、今日已提货、异常订单数量；
- 当前站点名称、营业时间、地址和联系电话；
- 快捷入口：扫码收货、手动收货、扫码提货、订单查询、异常登记；
- 显示网络状态和最近同步时间，断网时禁止确认写操作。

### 3.2 到站收货

收货是站点端的第一核心功能：

1. 扫描配送批次码、订单二维码或输入订单号；
2. 校验订单属于当前自提点、已完成支付或积分扣减、未取消/退款、履约方式为自提；
3. 展示商品明细、数量、用户昵称/脱敏手机号和订单状态；
4. 工作人员确认实际收到后，记录到站时间、操作人、站点和货物状态；
5. 生成或填写货架/储位号，状态变为“已到站，待用户提货”；
6. 支持批量收货，批量操作必须逐单返回成功、失败和失败原因。

收货异常包括：订单不存在、订单不属于本站、未支付、已取消、已退款、重复收货、商品缺失、商品破损和数量不符。异常只能登记，不能由站点人员直接修改订单金额或用户资产。

### 3.3 站内待提货

- 按待提货、已逾期、异常和已完成筛选；
- 支持订单号、手机号后四位、用户昵称和储位号查询；
- 展示到站时间、保管时长、提货截止时间和提醒状态；
- 支持发送“已到站待提货”通知，通知动作需要幂等；
- 逾期订单进入提醒列表，由运营或客服处理，不自动销毁订单。

### 3.4 用户提货核验

用户到站后由工作人员完成核验：

1. 扫描用户订单取货码，或输入订单号和取货码；
2. 校验取货码、订单、站点、支付状态和当前履约状态；
3. 展示商品、数量、用户脱敏信息和异常提示；
4. 工作人员确认交付后提交核验；
5. 订单变为 `picked_up`/`completed`，记录核验时间、操作人和站点；
6. 重复核验、错误码、错误站点和已退款订单必须拒绝。

站点端只验证取货码并确认交付，不允许跳过支付、修改订单商品、补录用户积分或直接审批退款。

### 3.5 异常与交接

- 错站订单：登记后转运营处理；
- 货损/缺货：上传图片、填写数量和说明，进入异常补偿队列；
- 用户信息不一致：暂停提货，转客服复核；
- 取货码无效或已使用：拒绝操作并保留审计记录；
- 设备/网络故障：允许查看缓存的只读信息，恢复网络后重新查询，禁止离线确认；
- 班次交接：记录未提货数量、异常数量、储位和交接人员。

## 4. 页面规划

| 页面 | 主要内容 |
| --- | --- |
| 站点登录 | 账号、密码/验证码、站点选择、退出登录 |
| 工作台 | 站点概况、待办数量、快捷扫码入口 |
| 收货扫码 | 摄像头扫码、手动输入、订单校验、确认收货 |
| 待提货列表 | 搜索、筛选、储位、到站时间、逾期提醒 |
| 提货核验 | 扫码/输入取货码、商品明细、确认交付 |
| 异常登记 | 异常类型、图片、数量、说明、提交记录 |
| 操作记录 | 收货、提货、异常、失败原因和操作人 |
| 站点设置 | 仅管理员可查看营业时间、联系电话和储位规则 |

移动端适合使用手机摄像头扫码；桌面端适合连接扫码枪和打印设备。两端使用同一套站点 API 和权限模型。

## 5. 建议接口

当前项目已有 `/api/admin/orders/:orderId/pickup-verify` 和自提点管理接口，但它们属于后台权限范围。站点端应新增隔离的 `/api/station/*` 接口：

```text
POST /api/station/auth/login
GET  /api/station/me
GET  /api/station/dashboard
GET  /api/station/orders?status=&keyword=&siteId=
POST /api/station/orders/:orderId/receive
POST /api/station/orders/:orderId/pickup-verify
POST /api/station/orders/:orderId/exceptions
GET  /api/station/operation-logs
```

接口必须服务端校验：站点归属、订单履约方式、支付/积分状态、订单状态、取货码、幂等键和操作权限。站点端不直接调用 `/api/admin/*`。

## 6. 数据补充

现有 `pickupSites` 和订单字段可以作为基础，建议增加：

```js
stationOrder: {
  orderId,
  siteId,
  stationStatus: "expected | received | ready | picked_up | exception",
  shelfCode,
  receivedAt,
  receivedBy,
  pickedUpAt,
  pickedUpBy,
  exceptionType,
  exceptionRemark
}
```

收货和提货操作都写入不可篡改的操作日志，并使用请求幂等键避免重复入站、重复核验和重复通知。

## 7. 与小程序的关系

- 用户小程序：查看订单“待到站/已到站/已提货”状态、查看取货码和自提点信息；
- 站点端：工作人员扫码收货和核验提货；
- 运营后台：配置自提点、分配人员、处理异常和查看全局履约；
- 站点端可做成独立 Web/H5 `/station`，也可做成独立工作人员小程序页面；不建议直接放进普通用户小程序页面中，以免混淆用户权限。

## 8. 开发优先级

### P0：收货和提货闭环

- 站点登录和站点权限；
- 待收货列表；
- 扫码/手动收货；
- 待提货列表；
- 扫码/手动提货核验；
- 操作日志和重复操作拦截。

### P1：站点履约增强

- 批量收货；
- 储位管理；
- 到站通知；
- 逾期提醒；
- 货损、缺货、错站异常登记；
- 班次交接。

### P2：运营和设备能力

- 扫码枪适配；
- 取货单打印；
- 站点履约报表；
- 多站点调拨和跨站异常处理。

## 9. 验收标准

- 站点人员只能看到授权站点的订单；
- 未支付、已取消、已退款和非本站订单不能收货或提货；
- 同一订单重复收货、重复提货不会重复改变状态；
- 错误取货码不能完成提货；
- 收货后用户端能看到“已到站待提货”；
- 提货核验后用户端和后台同步显示“已提货/已完成”；
- 异常操作有类型、说明、操作人、时间和订单关联；
- 站点人员不能修改商品价格、支付金额、用户积分或直接审批退款。
