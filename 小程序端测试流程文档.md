# TGG Shop 小程序端测试流程文档

> 版本：v1.0  
> 适用范围：`wechat-miniprogram/`  17 个页面及其后端接口  
> 测试目标：确认用户可以完成登录、浏览、积分兑换、会员购买、履约、售后和积分任务闭环。

## 1. 测试准备

### 1.1 工具和环境

1. 使用微信开发者工具导入 `wechat-miniprogram/`，AppID 使用项目配置中的 AppID。
2. 开发版 API 地址来自 `wechat-miniprogram/config/environments.js`。本地联调时仅在开发版使用 `tgg_config` 覆盖 HTTPS 地址；体验版和正式版使用已配置的线上地址。
3. 真机测试前，将 API 域名加入微信公众平台的 request、uploadFile、downloadFile 合法域名，并确认 HTTPS 证书有效。
4. 编译前执行“清缓存 → 清除编译缓存 → 重新编译”，再检查控制台首条异常。
5. 后端健康检查：`GET /api/health`；本地网页服务默认地址为 `http://127.0.0.1:5177`。

### 1.2 测试账号和数据

| 账号 | 密码 | 用途 |
| --- | --- | --- |
| `u_1001` | `123456` | 会员现金购物、积分补差 |
| `u_1002` | `123456` | 普通用户、纯积分兑换 |

准备至少一个上架现金商品、一个上架纯积分商品、一个库存为 0 的商品、一个可配送地址和一个启用自提点。每次回归记录测试前后的积分、库存和订单编号。

## 2. 冒烟流程

1. 打开小程序，首页、底部导航和 Banner 正常显示。
2. 登录后进入首页、商城、赚积分、购物车、我的，页面切换无白屏、无 JS 异常。
3. 打开一个商品详情，确认图片、名称、规格、价格、库存和购买入口完整。
4. 使用 `u_1002` 完成一次纯积分自提兑换，确认订单生成、积分扣减和核销码显示。
5. 使用 `u_1001` 完成一次现金商品购买，确认进入待配送/待自提状态。
6. 进入订单详情，确认订单状态、支付信息、履约方式和退款入口符合当前状态。

## 3. 功能测试流程

### 3.1 登录和会话

1. 使用正确账号登录，检查用户昵称、会员状态和积分余额。
2. 使用错误密码登录，确认显示明确错误且不会建立会话。
3. 退出后重新进入受保护页面，确认要求重新登录。
4. 刷新或重新打开小程序，确认有效会话可恢复；失效 token 应回到登录流程。
5. 检查登录、退出和刷新期间没有重复提交或重复创建订单。

### 3.2 首页、分类和商品

1. 首页检查位置、自提点、配送承诺、服务标签、Banner、推荐商品和纯积分入口。
2. Banner 点击应进入对应商品或分类；下架商品不得继续展示为可购买商品。
3. 分类页检查动态分类、综合/配送/现金/纯积分筛选和库存提示。
4. 商品详情检查规格、介绍、会员提示、纯积分“不支持现金补差”提示。
5. 库存为 0 时，加入购物车和立即购买按钮应禁用或给出已售罄提示。

### 3.3 购物车和结算

1. 现金商品加入购物车、增加/减少数量、删除商品，确认金额同步变化。
2. 纯积分商品加入兑换车，确认只显示积分金额，不出现现金补差入口。
3. 现金和纯积分商品混合时，确认页面阻止不符合规则的混合结算并给出提示。
4. 自提结算检查自提点、取货码和订单确认信息。
5. 配送结算检查地址、配送费、配送时段和默认地址。
6. 重复点击提交按钮，只允许生成一个订单。

### 3.4 会员、现金和积分规则

1. `u_1002` 点击现金商品，确认提示开通月会员，不得直接现金下单。
2. `u_1002` 兑换纯积分商品，确认无需会员且不显示现金补差。
3. `u_1002` 积分不足兑换时，确认显示积分不足，不出现现金支付入口。
4. `u_1001` 购买现金商品，确认生成现金支付场景订单。
5. 会员积分不足购买时，确认可按规则进入现金补差流程。
6. 会员开通和续费后，会员有效期、支付记录和页面状态同步更新。

### 3.5 订单、履约和售后

1. 检查待支付、已支付、待自提、配送中、已完成、取消等状态展示。
2. 自提订单确认取货码生成且只能由正确流程核销。
3. 配送订单确认地址、配送状态和收货按钮按状态开放。
4. 可退款订单提交退款申请，检查现金和积分按原支付来源展示。
5. 不可退款或重复提交退款时，确认按钮禁用并显示原因。
6. 订单列表、订单详情和支付记录中的订单号一致。

### 3.6 赚积分、签到和邀请

1. 任务列表不提前泄露需要在详情页查看的具体奖励值。
2. 任务详情、提交凭证、提交记录和审核状态可正常切换。
3. 签到检查连续签到、广告组展示、抽奖券和转盘结果。
4. 邀请关系只绑定一次，奖励和邀请记录可追踪。
5. 积分流水中的获得、消耗、退款、回滚与页面余额一致。

## 4. 异常和兼容性测试

- 断网后进入商品、提交订单、支付和上传页面，确认有明确失败提示且可重试。
- 快速连续点击登录、支付、下单、退款，确认幂等且不重复扣款/扣积分/扣库存。
- 切换前后台、锁屏恢复、弱网恢复，确认页面状态不会回退覆盖新数据。
- 检查 iOS、Android、不同屏幕尺寸、深色系统字体放大情况下无截断和横向溢出。
- 上传不支持的文件、超大文件或空文件，确认被拒绝且不会产生脏数据。

## 5. 验收标准

- 首页、商城、任务、订单、个人中心和登录页面均可打开。
- 普通用户不能现金购买；纯积分商品无需会员且禁止现金补差。
- 现金订单、纯积分订单均能完成创建、库存/积分扣减和履约状态流转。
- 重复提交、失效会话、库存不足、积分不足均有可理解提示。
- 真机无阻塞级崩溃；控制台无新增未处理异常。
- 测试记录包含账号、订单号、接口环境、截图和最终结论。

## 6. 测试记录

| 日期 | 版本/环境 | 账号 | 用例范围 | 通过数/失败数 | 缺陷编号 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

> 真实微信支付、退款、广告回调、任务平台回调和提现需要使用真实配置与真机/服务商联调；本地模拟通过不等于生产通道验收通过。
