# TGG Shop 后台运营端测试流程文档

> 版本：v1.0  
> 适用范围：运营后台 `dev/frontend/admin/`、后台 API、权限、配置和数据流水  
> 测试目标：确认运营人员可以安全完成商品、订单、履约、财务、权限和首页运营管理。

## 1. 测试准备

### 1.1 启动和地址

```powershell
cd dev/backend
npm run dev
```

- 后台地址：`http://域名/admin`
- 健康检查：`http://域名/api/health`
- 测试前确认数据库/JSON 存储为测试环境，避免使用生产数据。
- 测试写操作前记录原始配置、商品库存和订单编号；需要清理的测试数据使用独立名称。

### 1.2 账号和权限

| 角色 | 重点验证 |
| --- | --- |
| `super_admin` | 全模块访问、角色管理、配置和二次确认 |
| `product_admin` | 商品、纯积分商品、分类和库存；无财务审批权限 |
| `order_admin` | 订单和履约状态；不能改支付金额和用户余额 |
| `finance_admin` | 退款、提现、支付流水、积分流水和复核 |
| `customer_service` | 用户/订单查看、客服工单和备注；不能直接完成财务动作 |

本地演示密码默认 `123456`。每个角色使用独立浏览器会话，避免管理员 token 串用。

## 2. 冒烟流程

1. 使用 `super_admin` 登录，确认运营仪表盘、最近订单、指标卡、图表和筛选器正常加载。
2. 依次打开订单、商品、纯积分兑换、首页运营、权限管理页面，确认表格和操作按钮存在。
3. 查询一个商品和一个订单，确认详情、状态、库存和操作日志可见。
4. 保存一项可逆配置，确认二次确认、操作原因、保存提示和刷新后持久化。
5. 使用受限角色重新登录，确认无权限模块不显示可执行按钮，直接调用接口也返回 403。

## 3. 商品和库存闭环

### 3.1 新商品上架

1. 进入“商品与分类/商品上架与销售设置”，点击新增商品。
2. 填写名称、分类、单位、价格、积分价、库存、标签、介绍和操作原因。
3. 上传 PNG/JPEG/GIF/WebP 主图，确认文件头校验、上传地址回填和预览图片可见。
4. 点击“保存草稿”，确认状态为草稿/已下架，用户网页和公开 API 不可见。
5. 编辑草稿并点击“预览商品”，检查名称、图片、价格、库存、单位和介绍。
6. 点击“保存并上架”，确认上架前校验分类、主图、正库存和有效售价。
7. 在用户网页或小程序端刷新，确认商品可见、可购买；完成订单后确认库存扣减。
8. 在后台下架商品，确认用户端停止新交易，原订单和库存流水仍保留。

### 3.2 商品异常和权限

- 缺少主图、分类、价格或正库存时，上架应失败且商品资料不被部分保存。
- 已上架商品修改价格、分类、图片等展示资料时，必须先下架。
- 已上架商品可使用“调整库存”，必须填写盘点后的库存、当前库存和操作原因。
- 过期库存或 revision 提交应返回冲突，不覆盖其他人的新修改。
- 纯积分商品必须启用积分、关闭现金和现金补差，普通用户可兑换且无需会员。
- `product_admin` 无 `stock:write` 时不能调整库存；无 `points_product:write` 时不能维护纯积分商品。
- 非图片文件伪装为 `.png` 上传应被拒绝；超过 10 MB 应被拒绝。

## 4. 首页运营配置

1. 进入“首页运营”，检查 Banner 标题、副标题、关联商品、服务标签、配送承诺和活动入口。
2. 上传首页 Banner 图片，确认仅 `config:write` 角色可用，图片格式和大小校验生效。
3. 保存首页配置时填写操作原因并完成二次确认。
4. 刷新后台确认配置持久化，再刷新用户网页/小程序确认 Banner 图片、文案和活动入口同步。
5. 删除或更换活动入口，确认用户端入口数量和跳转页面同步。
6. 关联商品下架后，确认首页不提供失效商品的购买入口。

## 5. 订单和履约

1. 订单列表按状态、支付方式、履约方式筛选，确认订单金额、用户、商品、库存和支付信息一致。
2. 按状态机执行待支付、已支付、待自提、配送中、已完成、取消等允许动作。
3. 自提订单执行核销，确认取货码、核销人和时间写入日志。
4. 配送订单执行发货/配送/送达，确认配送员、配送状态和订单状态同步。
5. 非法状态跳转、他人订单操作、重复操作应被拒绝且不改数据。
6. 检查订单状态日志、库存流水和操作日志可追溯。

## 6. 财务、积分和审批

1. 财务页检查支付流水、积分流水、幂等键、用户和业务单号。
2. 退款按现金和积分拆分审批，确认二次复核后才能执行敏感动作。
3. 提现按提交、复核、服务商处理和回调状态检查；金额不一致或未知状态不得自动结算。
4. 月度积分奖励执行结算、二级复核、追回和对账，确认用户余额、流水和操作日志一致。
5. 普通客服角色不能直接完成退款、提现、人工积分或异常补偿。
6. 重复提交同一审批/回调/补偿请求应幂等，不重复写流水。

## 7. 权限和审计

1. 权限矩阵检查角色名称、权限勾选、保存、刷新持久化和操作原因。
2. 超级管理员角色不可被普通角色修改，当前登录角色不可修改自身权限。
3. 受限角色进入无权限页面时只读或隐藏按钮；直接请求接口必须返回 403。
4. 商品上下架、库存、配置、角色、退款、提现、补偿等写操作均应产生操作日志。
5. 检查日志包含操作者、角色、对象、前后值、原因和时间。

## 8. 回归命令和验收

在 `dev/backend` 目录执行：

```powershell
npm run db:check
npm run api:check
npm test
```

验收标准：

- 仪表盘、首页运营、商品上架、权限管理页面可加载且操作按钮可用。
- 商品完成草稿 → 编辑 → 预览 → 上架 → 用户购买 → 扣库存 → 下架闭环。
- 权限按商品、纯积分、库存、订单、财务、配置和角色边界隔离。
- 敏感操作需要原因和二次确认，异常和重复请求不产生重复流水。
- 迁移、OpenAPI、单元测试、JSON/SQLite/PG 冒烟和浏览器 E2E 均通过。

## 9. 测试记录

| 日期 | 后台角色 | 环境 | 测试模块 | 通过数/失败数 | 缺陷编号 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |
